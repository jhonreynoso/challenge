import { PlayerPosition } from "../enum/stats.enum";


export const PlayerPositionList = [
    PlayerPosition.POINT_GUARD,
    PlayerPosition.SHOOTING_GUARD,
    PlayerPosition.SMALL_FOWARD,
    PlayerPosition.POWER_FOWARD,
    PlayerPosition.CENTER,
]